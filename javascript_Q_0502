Questionary 

Q. 삼항 조건 연산자와 삼항 연산자의 활용 방식은 무었인가? (?의 활용법)
    var result = x % 2 ? '홀수' : '짝수'; -> 이해가 안됩니다.
    var kind = num ? (num > 0 ? '양수' : '음수') : '영'; -> ...?

Q. + 연산자는 숫자, 문자열을 합칠수 있고 문자열 "3"값도 강제로 숫자로 변환한다고 알고있는데.
    var a = 10
    var b = "10"
    a+b = 1010이 나옵니다. 강제로 숫자로 변환하여 연산하는 조건은 어떤것인가요? 

Q. 심볼 타입은 데이터를 담거나 불러오거나를 하기 위해서만 사용하는지, 정확한 사용법과 작동 방식은? 

Q. 변수에 호이스티은 자바스크립트에 특징인가? 다른 언어도 같은 룰이 적용이 되는가? 
